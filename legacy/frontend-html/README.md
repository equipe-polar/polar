# Frontend HTML legado

Estas telas HTML/CSS/JS foram preservadas apenas como referencia visual e prototipo historico.

O frontend ativo do P.O.L.A agora fica em `apps/web` e usa React + TypeScript + Vite.

As telas deste diretorio nao devem ser usadas como frontend final, nao participam do build do workspace e nao devem receber novas regras de negocio. Qualquer melhoria de produto deve ser implementada no app React.
